# Lab 7 - E2E Testing

Name: Yusuf Damda (No Partner)

## Q1
I would probably place the automated test within a Github action that runs whenever code is pushed. This would help catch any weird bugs and also make sure the new code does not tamper with any of the previous features. 


## Q2
No, I would not use an end to end test to check if a function is returning the correct output because they are used for full user workflow and not a single function.

## Q3
Navigation mode analyzes the webpage while it is loading and all the metrics are based on the page load process. Snapshot on the other hand doesn't measure loading or interaction performance but rather analyzes the website based on the current state. It is mostly used to find accessibility issues.

## Q4 
1. Reduce unused JavaScript so the website loads faster and uses fewer resources.
2. Improve performance by reducing long main thread tasks that can slow down the page.
3. Improve font loading and reduce render blocking resources so content appears faster for users.

